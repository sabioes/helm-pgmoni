imagePullSecrets: 
  - pgimporter-dockerhub-secret
dockerHubCredentials: ${DOCKERCONFIGJSON}
ingress:
  enabled: true
  className: "nginx"
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: pgimporter.localhost
      paths:
        - path: /
          pathType: Prefix